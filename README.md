# wda-builder

Assorted iOS build helpers. CI assembles the toolchain on macOS runners and drops a heartbeat into `build_ok.txt` whenever a run goes green.

Mostly infrastructure glue - nothing interesting to see here.
